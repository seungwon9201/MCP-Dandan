{
  "name": "82ch",
  "version": "1.0.0",
  "description": "MCP Security Framework with Desktop UI",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others --handle-input \"npm run server\" \"npm run front\" --names \"server,front\" --prefix-colors \"blue,green\"",
    "server": "python server.py",
    "front": "cd front && npm run dev",
    "restore-config": "python ./transports/config_finder.py --restore",
    "install-all": "pip install -r requirements.txt && npm install && cd front && npm install",
    "kill-server": "FOR /F \"tokens=5\" %P IN ('netstat -ano ^| findstr :8282 ^| findstr LISTENING') DO taskkill /PID %P /F"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "all": "^0.0.0"
  }
}

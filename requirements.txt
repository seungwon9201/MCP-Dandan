# Core web framework
aiohttp>=3.9.0

# HTTP client library
requests>=2.31.0

# SSL certificate bundle (required for proper HTTPS verification)
certifi>=2024.0.0

# Async file I/O
aiofiles>=23.0.0

# Async SQLite database
aiosqlite>=0.19.0

# Mistral AI (for ToolsPoisoningEngine)
mistralai>=1.0.0

# YARA for PII detection (for PIIFilterEngine)
yara-python>=4.5.0

# Environment variable management
python-dotenv>=1.0.0

# Windows console control handler (for graceful shutdown)
pywin32>=306; sys_platform == 'win32'
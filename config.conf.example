# 82ch-Engine Configuration (Refactored)
# Queue 관련 설정이 제거되었습니다.

[EventProvider]
# ZeroMQ Publisher 주소 (MCPCollector)
zmq_address = tcp://localhost:5555

[Engine]
# 활성화할 엔진 설정
sensitive_file_enabled = True
semantic_gap_enabled = True

[Log]
# 로그 디렉토리
log_dir = ./logs

# 로그 파일 최대 크기 (MB)
max_log_file_size_mb = 100

# 최대 로그 파일 개수 (오래된 파일 자동 삭제)
max_log_files = 5
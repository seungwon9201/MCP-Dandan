services:
  # 82ch - Unified MCP Security Framework
  app:
    build: .
    container_name: 82ch-server
    volumes:
      - ./data:/app/data          # Database storage
      - ./schema.sql:/app/schema.sql
      - ./.env:/app/.env          # Environment variables (optional)
    ports:
      - "28173:28173"             # Observer HTTP server
    environment:
      - PYTHONUNBUFFERED=1
      - MCP_PROXY_PORT=28173
      - MCP_PROXY_HOST=0.0.0.0
    restart: unless-stopped
    networks:
      - 82ch-network

networks:
  82ch-network:
    driver: bridge